import { Todolist } from './todolist';

describe('Todolist', () => {
  it('should create an instance', () => {
    expect(new Todolist('id', 'name', false)).toBeTruthy();
  });

  it('should create an instance', () => {
    expect(new Todolist('id', 'name', false)).toBeTruthy();
  });
});
